# Rename this file to .env and fill in your actual credentials.
# Do NOT commit the .env file to version control.

# OpenWeather API Key (get from https://openweathermap.org/api)
OPENWEATHER_API_KEY="your_openweather_api_key"

# Supabase Project URL (find in your Supabase project settings -> API)
SUPABASE_URL="your_supabase_url"

# Supabase Service Role Key (find in your Supabase project settings -> API)
# This key has admin privileges and should be kept secret.
# Used for backend operations like accessing/modifying data.
SUPABASE_KEY="your_supabase_service_role_key"

# Optional: Direct Database Connection URL (find in Supabase project settings -> Database -> Connection string)
# Used if connecting with psycopg2 directly instead of/in addition to supabase-py client.
# Format: postgresql://[user]:[password]@[host]:[port]/[database]
# Example: DATABASE_URL="postgresql://postgres:your-db-password@db.your-project-ref.supabase.co:5432/postgres"
DATABASE_URL=""

# For FastAPI development server
# HOST="127.0.0.1"
# PORT="8000"
